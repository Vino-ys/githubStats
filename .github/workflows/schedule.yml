name: Update Gist Stats

on:
  schedule: # 1시간마다 자동 실행
    - cron: "0 * * * *"
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  update-productive-box:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: maxheld83/ghaction-productive-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: ${{ secrets.GIST_ID_PRODUCTIVE }}
          TIMEZONE: Asia/Seoul # 시간대 설정

  update-stats-box:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: matchai/waka-box@master # github-stats-box 액션
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: ${{ secrets.GIST_ID_STATS }}
